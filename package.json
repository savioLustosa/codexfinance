{
  "name": "cedex-worksuite",
  "private": true,
  "version": "0.2.0",
  "description": "Plataforma SaaS de gestão de projetos e equipes com Supabase",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@cedex/frontend & npm run dev --workspace=@cedex/backend",
    "dev:frontend": "npm run dev --workspace=@cedex/frontend",
    "dev:backend": "npm run dev --workspace=@cedex/backend",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "release:check": "npm run lint && npm run typecheck && npm run test && npm run build"
  }
}
